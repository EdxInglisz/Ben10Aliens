{
  "name": "ben10-aliens-api",
  "version": "1.0.0",
  "description": "Ben 10 Aliens REST API",
  "scripts": {
    "start": "json-server db.json --host 0.0.0.0 --port ${PORT:-3000}"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
